{
  "name": "frontend",
  "version": "1.0.0",
  "main": "index.js",
  "license": "MIT",
  "scripts": {
    "serve": "rm -rf dist && parcel src/index.pug",
    "build": "rm -rf dist && parcel build src/index.pug"
  },
  "dependencies": {
    "svelte": "^3.12.0"
  },
  "devDependencies": {
    "node-sass": "^4.12.0",
    "parcel-bundler": "^1.12.3",
    "parcel-plugin-svelte": "^4.0.4",
    "pug": "^2.0.4",
    "svelte-preprocess": "^3.1.0",
    "typescript": "^3.6.2"
  },
  "browserslist": [
    "last 1 chrome versions"
  ],
  "alias": {
    "src": "./src"
  }
}
